#include <stdio.h>
#define PI 3.1416 /* definicion de constante */

int main() {
    float perim, radio; int dos = 2;
    printf("Calcula el perimetro de la circunferencia\n", n);
    printf("Indique el tamaño de la circunferencia: \n", n);
    scanf("%f", &radio);
    perim = dos * PI * radio;
    printf("\nEl perimetro de la circunferencia es %f\n", perim);
    printf("\n\nValores utilizados para calcular el perimetro:\n");
    printf("Constante PI = %f, valor de dos = %d, radio = %f\n\n", PI, dos, radio);
    return 0;   
}